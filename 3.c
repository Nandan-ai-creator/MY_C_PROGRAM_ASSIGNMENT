#include<stdio.h>
int main()
{
    printf("The main() function in c is the starting point of execution of the pgm\n");
    printf("without main() compiler will not execute the program\n");
    printf("it tells the compiler from where the  program execution starts\n");
    return 0;
    
}
